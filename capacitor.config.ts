import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.donkeysisle.naelBirthday',
  appName: 'naelBirthday',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
